server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Tất cả route frontend đều chuyển về index.html
    location / {
        try_files $uri /index.html;
    }

    # Serve static files
    location /assets/ {
        try_files $uri =404;
    }
}
